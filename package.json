{
  "name": "sysspecs-order-service",
  "version": "0.1.0",
  "private": true,
  "description": "Implementation of the sysspec `orders` service (see contracts.lock for the pinned contract surface)",
  "type": "commonjs",
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.2.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
